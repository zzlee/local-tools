#!/usr/bin/env node

const [,, ...args] = process.argv;

if (args.length === 0) {
  console.log("Welcome to Local Tools! Usage: my-tool <message>");
  process.exit(0);
}

console.log(`Local Tools says: ${args.join(" ")}`);
